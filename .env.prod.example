# Production Environment Variables for OneSpirit
# IMPORTANT: Copy this file to .env.prod and update with your actual values
# NEVER commit .env.prod to version control!

# Django Secret Key - Generate a secure key!
# Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=CHANGE-THIS-TO-A-SECURE-SECRET-KEY

# Domain Configuration
VIRTUAL_HOST=yourdomain.com
LETSENCRYPT_HOST=yourdomain.com,www.yourdomain.com
LETSENCRYPT_EMAIL=admin@yourdomain.com

# Allowed Hosts (comma-separated, no spaces)
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Database Configuration
DB_ENGINE=django.db.backends.postgresql
DB_NAME=onespirit_production
DB_USER=onespirit_prod_user
DB_PASSWORD=CHANGE-THIS-TO-A-SECURE-DATABASE-PASSWORD
DB_HOST=postgres
DB_PORT=5432

# Redis Configuration
# Note: Redis password is stored in secrets/redis_password.txt
# REDIS_URL will be constructed as redis://:PASSWORD@redis:6379/1
REDIS_URL=redis://redis:6379/1

# Email Configuration (Update with your SMTP settings)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-app-password
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
SERVER_EMAIL=server@yourdomain.com

# AWS S3 Configuration (Optional - for media storage)
# USE_S3=True
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_STORAGE_BUCKET_NAME=onespirit-media
# AWS_S3_REGION_NAME=us-east-1

# Sentry Configuration (Optional - for error tracking)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Performance Settings
GUNICORN_WORKERS=4
GUNICORN_THREADS=2
